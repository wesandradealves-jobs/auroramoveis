import React, { useCallback, useContext } from 'react';

import {  
	Container,
} from './styles';

const Checkout: React.FC = () => {
	return (
		<Container>
			Checkout
		</Container>
	);
};

export default Checkout;
